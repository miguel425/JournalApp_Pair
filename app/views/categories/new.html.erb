<%= form_with model: @category do |f| %>
  <div>
    <%= f.label :title %>
    <%= f.text_field :title %>
    <div>
    hello sample reload this should show up
    </div>
  </div>
  <%= f.submit 'Create Category' %>
<% end %>